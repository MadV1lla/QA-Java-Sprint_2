package model.constants;

public class Colour{
    public static double string(String a) {
        Discount discount = new Discount();
        if(a.equalsIgnoreCase("RED")){
            return discount.getDiscount();
        } return 1;
    }
}
